<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="BalanceSheet.DetailPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BalanceSheet"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <ScrollViewer Grid.Row="0" Grid.Column="0">
            <StackPanel x:Name="MainPanel"
                Margin="16,32,16,24"
                Spacing="16">
                <TextBlock Style="{StaticResource HeaderTextBlockStyle}"
                   x:Uid="DetailTitle"/>
                <TextBlock Style="{StaticResource BodyTextBlockStyle}" x:Uid="DetailDescriptions"/>
                <StackPanel Orientation="Horizontal"
                    Spacing="24">
                    <CalendarDatePicker x:Uid="Date"
                                Date="{x:Bind tayra.Date, Mode=TwoWay}"/>
                    <TimePicker x:Uid="TimeOfDay"
                        SelectedTime="{x:Bind tayra.TimeOfDay, Mode=TwoWay}"
                        ClockIdentifier="24HourClock"/>
                </StackPanel>
                <TextBox Text="{x:Bind tayra.Evt, Mode=TwoWay}"
                   x:Uid="Event"/>
                <NumberBox Value="{x:Bind tayra.Cash, Mode=TwoWay}"
                   x:Uid="Cash"/>
                <NumberBox Value="{x:Bind tayra.Icoca, Mode=TwoWay}"
                   x:Uid="Icoca"/>
                <NumberBox Value="{x:Bind tayra.Coop, Mode=TwoWay}"
                   x:Uid="Coop"/>
                <CommandBar Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
                    x:Uid="CmdBar">
                    <AppBarButton x:Name="ReturnBtn"
                          x:Uid="ReturnBtn"
                          Click="ReturnBtn_Click">
                        <AppBarButton.Icon>
                            <FontIcon Glyph="&#xE845;" />
                        </AppBarButton.Icon>
                    </AppBarButton>
                    <AppBarButton Icon="SaveLocal"
                          x:Uid="SaveCmdBtn"
                          Command="{x:Bind tayra.SaveCommand}"/>
                    <AppBarButton Icon="Delete"
                          x:Uid="DeleteCmdBtn"
                          Command="{x:Bind tayra.DeleteCommand}"/>
                    <AppBarButton Icon="Send"
                          Command="{x:Bind tayra.SaveChangesCommand}"
                          x:Uid="SaveChangesCmdBtn"/>
                </CommandBar>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
